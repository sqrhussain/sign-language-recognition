function bounding_box = elect_face(bboxes)
% TODO must be implemented to elect the region closest to skin color or most similar to faces
% now it just takes the first box as a face (which works fine)
bounding_box = bboxes(1,:);

end
